# 開発者向けガイド

## テスト

Python標準ライブラリだけで起動スクリプトを検証できます。

```bash
/bin/bash -n start_server.sh
python3 -m unittest discover -s tests -v
```

テストは一時ディレクトリ内のダミーモデルと代替サーバーコマンドを使用します。モデルのダウンロードや実際のMLXサーバー起動は行いません。

検証対象：

- `/bin/bash`による構文チェック
- モデル一覧、引数、空白を含むパスの処理
- Hugging Faceキャッシュの解決と、不完全・曖昧なキャッシュの拒否
- 起動引数、オフライン設定、設定値の検証

## CI

[GitHub Actions](.github/workflows/ci.yml)で、push・Pull Request・手動実行時にLinuxとmacOSの両方で同じ検証を行います。

MLXによる推論速度、音声、各アプリや実機との接続はCIの検証対象外です。これらに影響する変更では、必要な実環境で別途確認してください。

## 設定・連携を変更するとき

- `context_length`はMLXサーバーの起動引数ではありません。Hermesなどのクライアント設定との整合を確認してください。
- 起動時に読み込む設定の変更は、次回のサーバー起動から反映されます。
- エージェントの権限・確認・ツール制限は、各クライアント側で管理します。
